const CardColor = (type) => {
    switch (type) {
        case "fire":
            const main_style = 'bg-fire-500';
            return {main_style} ;
            case "fire":
                const main_style = 'bg-fire-500';
                return {main_style} ;
    }
}
 
export default CardColor;